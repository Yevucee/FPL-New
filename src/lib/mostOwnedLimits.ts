/** Public league page — top N players in the most-owned block. */
export const MOST_OWNED_PUBLIC_LIMIT = 15;

/** Planner page — show a few more for team-building context. */
export const MOST_OWNED_PLANNER_LIMIT = 20;

/** Differentials: owned by at most this many managers. */
export const DIFFERENTIAL_MAX_OWNERS = 2;

export const DIFFERENTIAL_PLANNER_LIMIT = 12;

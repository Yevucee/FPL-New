[build]
builder = "RAILPACK"
buildCommand = "npm ci && npm run build"

[deploy]
preDeployCommand = ["npm run db:migrate"]
startCommand = "bash scripts/start-with-history.sh"
healthcheckPath = "/ready"
healthcheckTimeout = 600
restartPolicyType = "ON_FAILURE"
